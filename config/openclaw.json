{
  // Janet - OpenClaw Configuration (Minimal valid config)

  gateway: {
    mode: "local"
  },

  agents: {
    defaults: {
      model: {
        primary: "anthropic/claude-opus-4-6"
      }
    }
  }
}
